const initialState = {
    pizzas: [],
    isLoading: false
}

const pizza = (state = initialState, action)  => {
    switch (action.type) {
        default:
            return state;
    }
}

export default pizza;
